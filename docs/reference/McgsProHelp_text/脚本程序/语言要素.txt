===  ===

11.3
语言要素
在
McgsPro
组态软件中，脚本程序使用的语言非常类似普通的
Basic
语言，本节将对脚本程序的语言要素进行详细的说明。在利用
McgsPro
语言要素进行编辑时，除了手动输入方式以外，还可以通过“对象和函数列表”栏选择输入到编辑栏。
11.3.1
变量和常量
（
1
）变量
McgsPro
组态软件脚本程序中，用户不能定义子程序和子函数，脚本编辑器中的变量可以是：
l
实时数据库中组态的数据对象。
数据对象可以看作是脚本程序中的全局变量，在所有的程序段共用。脚本编辑中可以用数据对象的名称来读写数据对象的值，也可以对数据对象的属性进行操作。其支持整数、浮点数、字符串三种数据对象。
l
当前脚本中定义的局部变量。
其支持字节型、整数、浮点数、字符串四种数据类型。
l
当前脚本中定义的局部数组变量。
其支持字节型数组、整数数组、浮点数数组、字符串数组四种数据类型。
其访问元素的方式为
array[index]
，其中“
array
”为数组变量、“
index
”为访问元素的位置（
从
1
开始
），返回对应的元素类型：字节型（
byte
）、整数（
integer
）、浮点数（
float/single
）、字符串（
string
）。
在脚本程序中不能对组对象进行读写操作，但可以对组对象的方法进行操作（如存盘方法函数）。
（
2
）常量
l
整数常量：类似
123
的整数
l
十六进制整数常量：类似
0x123
的十六进制整数
l
浮点数常量：带小数点的数值，如：
12.45
l
字符串常量：英文双引号内的字符串，如：
"OK ", "
正常
"
l
字符串常量内支持以下转义：
“
\n
”：代表换行
“
\r
”：需要和“
\n
”一起使用（“
\r\n
”），在微软环境中代表换行
“
\"
”：代表双引号
“
\t
”：代表制表符
“
\\
”：代表反斜杠自身
其他以“
\”
开始的字符将作为非法字符串。
（
3
）系统变量
McgsPro
组态定义的内部数据对象作为系统内部变量，在脚本程序中可自由使用，在使用系统变量时，变量的前面必须加“
$
”符号，如
$Date
。系统变量为只读类型，在脚本编辑环境中，点击展开“对象和函数列表”中的“系统变量”，再双击变量名称即可添加到编辑框中。
（
4
）属性和方法
属性和方法都是相对于
MCGS
对象而说的，引用对象的方法可以参见下个部分。
11.3.2
对象
McgsPro
组态软件的对象形成一个对象树，
MCGS
对象是最根部的对象，它的属性就是系统变量，它的方法就是系统函数。
MCGS
对象下面有“用户窗口”对象、“设备”对象、“数据对象”
等子对象。“用户窗口”以各个用户窗口作为子对象，每个用户窗口对象以这个窗口里的构件作为子对象。对象树形结构可以通过脚本编辑环境中“对象和函数列表”展开观看。
使用对象的方法和属性，必须要引用对象，然后使用点操作来调用这个对象的方法或属性。为了引用一个对象，需要从对象根部开始引用，这里的对象根部，是指可以公开使用的对象。
MCGS
对象，用户窗口、设备和数据对象都是公开对象，因此，语句
InputETime =
$Time
是正确的，而语句
InputETime = MCGS.$Time
也是正确的，同样，调用函数
!Beep()
时，也可以采用
MCGS.!Beep()
的形式。可以写：窗口
0.Open(),
也可以写：
MCGS.
用户窗口
.
窗口
0.Open()
，还可以写：用户窗口
.
窗口
0.Open()
。但是，如果要使用控件，就不能只写：控件
0.Left
，而必须写：窗口
0.
控件
0.Left
，或：用户窗口
.
窗口
0.
控件
0.Left
。
在“对象和函数列表”中，双击需要的方法和属性，
MCGS
将自动生成最小可能的表达式。
11.3.3
事件
在
McgsPro
组态软件的动画界面组态中，可以组态处理动画事件。动画事件是在某个对象上发生的，它可能是带参数也可能是不带参数的动作驱动源。窗口对象的事件可
参考7.7
节
，构件对象的事件可
参考8.2
节
。
11.3.4
表达式
由变量、括号和各种运算符组成的运算式称为表达式，表达式的计算结果称为表达式的值。
当表达式中包含有逻辑运算符或比较运算符时，表达式的值只可能为
0
（条件不成立，假）或非
0
（条件成立，真），这类表达式称为逻辑表达式；当表达式中只包含算术运算符，表达式的运算结果为具体的数值时，这类表达式称为算术表达式；常量或变量是狭义的表达式，这些单个量的值即为表达式的值。表达式值的类型即为表达式的类型，必须是字节型、整数、浮点数、字符串四种类型中的一种。
表达式是构成脚本程序的最基本元素，也常常应用于构件对象的组态中。
11.3.5
联行符
在语句结束使用“
_
”可连接下一行至本行，共同形成一条完整的语句。使用规则：
<
空格
>_<
回车或注释
>
，如：
a = a + _
b
将形成等效语句：
a = a + b
注意：
（
1
）联行符不能用来连接字符串，正确的方式是使用加法和联行符一同来完成：
str = "aaaaa" +
_
"bbbbb"
（
2
）当前行，联行符必须是最后一个有效字符。
11.3.6
运算符
表
11.1
中列出了
McgsPro
组态软件脚本编程中所支持的运算符：
表
11.3-1
运算符列表
分类
名称
运算符
算术运算符
乘方
^
乘法
*
除法
/
整除
\
加法
+
减法
-
取余
MOD
逻辑运算符
与
AND
非
NOT
或
OR
异或
XOR
比较运算符
大于
>
大于等于
>=
等于
=
小于等于
<=
小于
<
不等于
<>
（
1
）其中
AND
、
OR
为短路执行，即：
l
AND
：
exp1 AND exp2
，当
exp1
求值为
0
时，
exp2
将不会被执行
l
OR
：
exp1 OR exp2
，当
exp1
求值为非
0
时，
exp2
将不会被执行
（
2
）算术类型做算术操作和逻辑操作时，将按如下类型提升：
byte->integer->float
l
比如：
integer + float -> float + float
l
类型提升仅由当前语句影响，如：
float = integer + integer
，仅会在“
integer+integer
”（第一个语句）完成后，才会提升为
float
，然后赋值（第二个语句）。（
此处与
WinCE
不同
）
（
3
）在传值（赋值
/
函数传参）时，类型不匹配会发生自动的可能不安全的类型转换
l
byte/integer/float
会相互转换（可能会有值丢失）
l
string
不会
（
4
）字符串使用比较运算符时，区分大小写
（
5
）运算符优先级
表
11.2
所示为运算符的优先级：
表
11.3-2
运算符优先级
优先级
运算符
高
低
( )
^
*  /  \
   MOD
+  -
<  >
   <=  >=  =  <>
NOT
AND  OR  XOR
11.3.6
系统函数
作为脚本语言的一部分，
McgsPro
提供了几组系统函数，这些函数主要不是作为组态软件的功能提供的，而是为了完成脚本语言的功能提供的。点击展开“对象和函数列表”中的“系统函数”，双击选择对应函数即可添加到编辑框中。这些函数的意义及其使用请参考
第十二章
。