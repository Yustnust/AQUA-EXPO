===  ===

12.7
系统变量
McgsPro
内部定义了一些变量，我们称之为
McgsPro
系统变量。在进行组态时，可直接使用这些系统变量。为了和用户自定义的变量相区别，系统变量的名称一律以“
$
”符号开头。
McgsPro
系统变量多数用于读取系统内部设定的参数，它们只有值的属性，没有最大值、最小值及报警属性。
Ø
$Year
Ø
$Month
Ø
$Day
Ø
$Hour
Ø
$Minute
Ø
$Second
Ø
$Week
Ø
$Date
Ø
$Time
Ø
$Timer
Ø
$RunTime
Ø
$PageNum
Ø
$UserName
Ø
$MaxInput
Ø
$MinInput
Ø
$OrgInput
Ø
$CurInput
Ø
$PinyinInput
Ø
$SpellMatch
$Year
对象意义：
读取计算机系统内部的当前时间：“年”（
1111
～
9999
）
对象类型：
浮点数
读写属性：
只读
$Mont
h
对象意义：
读取计算机系统内部的当前时间：“月”（
1
～
12
）
对象类型：
浮点数
读写属性：
只读
$Day
对象意义：
读取计算机系统内部的当前时间：“日”（
1
～
31
）
对象类型：
浮点数
读写属性：
只读
$Hour
对象意义：
读取计算机系统内部的当前时间：“小时”（
0
～
23
）
对象类型：
浮点数
读写属性：
只读
$Min
ute
对象意义：
读取计算机系统内部的当前时间：“分钟”（
0
～
59
）
对象类型：
浮点数
读写属性：
只读
$Sec
ond
对象意义：
读取当前时间：“秒数”（
0
～
59
）
对象类型：
浮点数
读写属性：
只读
$We
ek
对象意义：
读取计算机系统内部的当前时间：“星期”（
1
～
7
）
对象类型：
浮点数
读写属性：
只读
$D
ate
对象意义：
读取当前时间：“日期”，字符串格式为：（年
-
月
-
日），年用四位数表示，
月日用两位数表示，如
1997-01-09
对象类型：
字符串
读写属性：
只读
$Ti
me
对象意义：
读取当前时间：“时刻”，字符串格式为：（时
:
分
:
秒），时、分、秒均用
两位数表示，如
20:12:39
对象类型：
字符串
读写属性：
只读
$Tim
er
对象意义：
读取自午夜以来所经过的秒数
对象类型：
浮点数
读写属性：
只读
$Ru
nTime
对象意义：
读取应用系统启动后所运行的秒数，小数部分表示毫秒值
对象类型：
浮点数
读写属性：
只读
$Pag
eNum
对象意义：
表示打印时的页号，当系统打印完一个用户窗口后，
$PageNum
值自动加
1
。用户可在用户窗口中用此变量来组态打印页的页号
对象类型：
浮点数
读写属性：
读写
注意事项：
McgsPro
系列产品该变量恒为
0
，无意义
$UserNa
me
对象意义：
在程序运行时记录当前用户的名字。若没有用户登录或用户已退出登录，“
$UserName
”为空字符串
对象类型：
内存字符串型变量
读写属性：
只读
$
MaxInput
对象意义：
在程序运行时记录激活的输入键盘的最大值。若激活的输入键盘是用来输入字符串，“
$MaxInput
”为空字符串
对象类型：
内存字符串型变量
读写属性：
只读
$
MinInput
对象意义：
在程序运行时记录激活的输入键盘的最小值。若激活的输入键盘是用来输入字符串，“
$MinInput
”为空字符串
对象类型：
内存字符串型变量
读写属性：
只读
$
OrgInput
对象意义：
在程序运行时记录激活的输入键盘的初始值。
对象类型：
内存字符串型变量
读写属性：
只读
$CurInput
对象意义：
在程序运行时记录激活的输入键盘的当前值。
对象类型：
内存字符串型变量
读写属性：
只读
$PinyinInput
对象意义：
当使用自定义系统键盘并切换到中文输入法时，显示当前输入的拼音音节。
对象类型：
内存字符串型变量
读写属性：
只读
$SpellMatch
对象意义：
当使用自定义系统键盘并切换到中文输入法时，显示当前拼音音节匹配的汉字列表。
对象类型：
内存字符串型变量
读写属性：
只读